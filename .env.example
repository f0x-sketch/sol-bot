# Solana RPC and WebSocket URLs
# Replace with your actual QuickNode and Helius HTTP/S endpoints
QUICKNODE_RPC_URL="https_your_quicknode_http_endpoint_here"
HELIUS_RPC_URL="https_your_helius_api_key_http_endpoint_here"
# SOLANA_WS_RPC_URL="wss_your_websocket_endpoint_here" # Optional, if used directly

# OpenRouter API Key (https://openrouter.ai/keys)
OPENROUTER_API_KEY="sk-or-v1-..."

# Wallet Private Key (CRITICAL - HANDLE WITH EXTREME CARE)
# This should be a comma-separated string of bytes for your wallet's private key.
# Example: If your private key bytes are [10, 20, 30, ... , 250], then:
# WALLET_PRIVATE_KEY_PLACEHOLDER="10,20,30,...,250"
# DO NOT COMMIT THIS FILE WITH YOUR ACTUAL PRIVATE KEY.
# For security, consider loading this from a more secure secret management system for production.
WALLET_PRIVATE_KEY_PLACEHOLDER="YOUR_PRIVATE_KEY_AS_COMMA_SEPARATED_BYTES_HERE"

# Optional: You can add other configurations here if needed
# JUPITER_API_VERSION="v6" (already in code, but could be env var)
# MIN_PROFIT_THRESHOLD_USD="0.05" (already in code)
# SLIPPAGE_BPS="50" (already in code)
